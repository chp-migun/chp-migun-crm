# migun.lsp — כלי נספחי המיגון לאוטוקאד

## התקנה (פעם אחת)
1. שמור את `migun.lsp` בתיקייה קבועה (למשל `C:\migun\`).
2. פתח את `migun-lib.dxf` באוטוקאד ושמור אותו כ-`migun-lib.dwg` באותה תיקייה
   (הקובץ נבנה מספריית הבלוקים של המשרד — פרטי פתחים, חתכים, סימוני זיון).
3. **הוסף את התיקייה ל-Trusted Paths**: `OPTIONS` → לשונית `Files` →
   `Trusted Locations` → `Add...` → הוסף את התיקייה שבחרת בשלב 1.
   בלי זה `APPLOAD` יעבוד רק בהנחה שיאשרו ידנית "Load Once", ו-headless
   דרך `accoreconsole` ייכשל עם `File load canceled` (`SECURELOAD`).
4. באוטוקאד: `APPLOAD` → בחר את `migun.lsp` → Load.
   כדי שייטען אוטומטית בכל פתיחה: הוסף אותו ל-Startup Suite באותו חלון.

### בדיקה headless דרך accoreconsole (אופציונלי, לפיתוח)
`test-mmdtest.scr` בתיקייה זו טוען את migun.lsp ומריץ MMDTEST בלי GUI:
```powershell
& 'C:\Program Files\Autodesk\AutoCAD 2021\accoreconsole.exe' `
    /s 'C:\migun\repo\mamad-annex\lisp\test-mmdtest.scr' /l en-US |
    ForEach-Object { $_ -replace "`0","" }
```
מחפשים בפלט את השורה `RESULT: PASS`.

**שים לב — נתיב מלא ל-`/s`.** נתיב יחסי נכשל בשקט: accoreconsole מדפיס
`"...scr": Can't find file.`, ממשיך לרוץ ויוצא בקוד הצלחה — כלומר נראה
כאילו הכל עבר, בלי שהורץ ולו טסט אחד.

בסקריפט יש `(setvar "SECURELOAD" 0)` כרשת ביטחון במקרה שהתיקייה עוד
לא ב-Trusted Paths. השורה `; error: Function cancelled` בסוף הריצה תקינה —
היא מגיעה מ-`QUIT` שסוגר את האפליקציה באמצע הפקודה.

### הפקת DWG הדגמה (בלי לפתוח אוטוקאד)
```powershell
& 'C:\Program Files\Autodesk\AutoCAD 2021\accoreconsole.exe' `
    /s 'C:\migun\repo\mamad-annex\lisp\make-demo-dwg.scr' /l en-US
```
מייצר `C:\migun\out\mamad-annex-demo.dwg` — נספח הדגמה עם שני ממ"דים,
חתכים, טבלה מרכזת והערות. לפתיחה רגילה באוטוקאד ובדיקה ויזואלית.

## עבודה שוטפת
| פקודה | מה היא עושה |
|---|---|
| `MMDDIM` | קליק אחד בתוך המרחב שבגזירה. אוטוקאד מתחקה את קו הפנים, הכלי מנקה רעש (קפיצות מזוזה, קודקודים על קו ישר), מודד את עובי כל קיר, ומסמן: מידת פנים לכל צלע, עובי לכל קיר, ובמרכז — שם, שטח במ"ר ונפח במ"ק. |
| `MMDCUT` | **קליק אחד בתוך המרחב.** אוטוקאד מתחקה בעצמו את קו הפנים (‎-BOUNDARY), הכלי מודד שטח והיקף, מודד את עובי כל קיר מהשרטוט, וגוזר את הגאומטריה של האדריכל אל הנספח **בגודל אמיתי** (1:1). קנה המידה 1:50 נקבע בוויופורט בפריסה, לא בהגדלת השרטוט. תופס נישות וכיסים — אין הנחת מלבן. |
| `MMD` | סימון מרחב מוגן בתוכנית האדריכלית: 3 קליקים לפינות, קליק לכל עובי קיר, ואז דלת/חלון/צינור בקליקים. הנתונים נשמרים בתוך ה-DWG. |
| `MMDDRAW` | מייצר את הנספח: תוכנית לכל מרחב עם קירות/פתחים/הצללה/מידות, חתך לכל מרחב, טבלה מרכזת והערות כלליות. |
| `MMDDETAILS` | מזריק את פרטי הפתחים של המשרד (פרט פתח חלון / פרט פתח דלת) לכל פתח שנאסף. |
| `MMDLIB` | טוען את ספריית הבלוקים לשרטוט הנוכחי (נדרש פעם אחת לכל DWG). |
| `MMDLIST` / `MMDDEL` / `MMDCLR` | רשימה / מחיקה / ניקוי. |

בזמן האיסוף הכלי מדפיס התראות `!!` כשמשהו חורג מכללי פקע"ר
(רוחב דלת לא תקני, צינור קרוב מדי לפתח, גובה צינור נמוך מדי...).
ההתראות לא חוסמות — אתה המחליט.

## איך יודעים שאוטוקאד טען את הגרסה הנכונה
`migun.lsp` נושא **חותמת בנייה** — 8 תווים שנגזרים מתוכן `migun.lsp.src`.
אותו מקור נותן תמיד אותה חותמת.

1. בשורת הפקודה של אוטוקאד, כשהקובץ נטען, מופיע:
   `=== MIGUN.LSP נטען   (build 611c4899) ===`
2. `MMDTEST` מדפיס אותה שוב בשורת `Build`.
3. בצד הפיתוח, `python build_lsp.py` מדפיס את החותמת הצפויה.

חותמות שונות = אוטוקאד טען קובץ ישן מתיקייה אחרת. אז: `APPLOAD` →
`Loaded Applications` יראה את הנתיב המלא של מה שנטען בפועל.

## הנחות
- השרטוט בס"מ (`*MG-UNITS*` בראש הקובץ — לשינוי אם עובדים במטרים).
- הפקודות עובדות גם על תוכנית מסובבת — הכיוון נקבע בקליק השני.
- `migun.lsp` נבנה מ-`migun.lsp.src` (UTF-8) עם `python3 build_lsp.py`.
  אין לערוך את migun.lsp ישירות — עורכים את ה-src ובונים מחדש.

## למה ככה
- כל הגאומטריה ב-`entmake` — לא מושפע מ-OSNAP/ORTHO/Dynamic Input.
- עברית לישויות נכתבת כ-`\U+XXXX` (חסין קידוד); הודעות מסך ב-cp1255.
- משתני מערכת נשמרים ומוחזרים, כולל בביטול באמצע (`*error*` handler).
